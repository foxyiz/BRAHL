# x — capability catalog

`xCapa.csv` lists every **ActionType / ActionName** the engine can run.

When you author `y/<suite>/y2Actions.csv`, each row’s `ActionName` must match a row here.

Handlers are **frozen** inside `f/FoXYiZ.exe` + `f/_internal/`. This folder is reference only.

Package docs: `_Docs/FoXYiZ.md` · `_Docs/BRAHL.md` · `_Docs/QAonAir.md`
